1) shell 是命令解释器，也是用户和内核进行交互的界面
   1.1) 让我们有地方输入命令
   1.2）shell 可以作为脚本语言
   1.3）shell 可以直接调用系统命令，这是对其他一切语言的优势 <<======================>>
   所以对系统管理员最方便

# windows 的图像交换界面实际上就是windows的shell

Bourne Shell   .sh    # 1979年发行的最古老shell， 也叫 B shell
bash           .bash  # 与sh 兼容，也是linux 的主流 sehll  
csh                   # 主要 bsd  语法和其他完全不同， 兼容C 语言
zcsh
zsh

# sudo cp .bashrc /root

sudo cat /etc/shells

/bin/sh
/bin/bash
/usr/bin/sh
/usr/bin/bash

sh  # go to Boune Shell
exit # return bash

# uid from 1000 ==> gao
